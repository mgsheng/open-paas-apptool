spring:
  cloud:
    consul:
      host: 10.96.142.132
      port: 8500
      discovery:
        healthCheckPath: /info
        healthCheckInterval: 15s
        instance-id: apptool-service
        hostname: localhost
        register: true
  application:
     name: apptool-service
server:
  port: 8080
ribbon:
   ServerListRefreshInterval: 1000
endpoints:
    health:
      sensitive: false
    restart:
      enabled: true
    shutdown:
      enabled: true

